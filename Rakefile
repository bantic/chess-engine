# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'chess' do
  developer('cory forsyth', 'cory.forsyth@gmail.com')

  # self.rubyforge_name = 'chessx' # if different than 'chess'
end

# vim: syntax=ruby
